# Scripts

Place local setup, dataset validation, training, migration, seed, and demo utility scripts here as they are implemented.

Do not add scripts that install dependencies, download models, or start long training jobs without documenting the command and getting user approval first.